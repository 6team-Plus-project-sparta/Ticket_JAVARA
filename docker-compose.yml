# version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: ticketjavara-mysql
    environment:
      MYSQL_DATABASE: ticketjavara
      MYSQL_ROOT_PASSWORD: 1234
      TZ: Asia/Seoul
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  redis:
    image: redis:7.0
    container_name: ticketjavara-redis
    ports:
      - "6379:6379"

  # app:
  #   build: .
  #   container_name: ticketjavara-app
  #   depends_on:
  #     - mysql
  #     - redis
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     SPRING_PROFILES_ACTIVE: local
  #     SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/ticketjavara?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
  #     SPRING_DATASOURCE_USERNAME: root
  #     SPRING_DATASOURCE_PASSWORD: 1234
  #     SPRING_DATA_REDIS_HOST: redis
  #     SPRING_DATA_REDIS_PORT: 6379
  #     JWT_SECRET: local-dev-secret-key-minimum-256-bits-long-placeholder

volumes:
  mysql_data: